{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <section class="auth-wrapper">
        <div class="auth-card text-center">
            <span class="badge">Espace utilisateur</span>
            <h1 class="page-title">Connexion</h1>
            <p class="page-subtitle">Cette page n'est plus utilisée.</p>

            <div class="mt-2">
                <a href="{{ path('app_user_login') }}" class="btn btn-primary">Aller à la vraie page de connexion</a>
            </div>
        </div>
    </section>
{% endblock %}